# FlexifyAI
Flexify.ai is a no-code AutoML web app that builds, trains, and evaluates ML models from a dataset upload. It automates EDA, target detection, preprocessing, algorithm selection, and tuning, generating visual reports with key metrics—democratizing machine learning for all users.
